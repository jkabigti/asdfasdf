@{
    ViewBag.Title = "Edit";
    var id = ViewBag.id;
}
<h2>Edit</h2>

<div id="divAdminEdit">
    First : <input type="text" data-bind="value: $data.first" />
    Ex: John
    <br />
    <br />
    Last : <input type="text" data-bind="value: $data.last" />
    Ex: Doe
    <br />
    <br />
    Email : <input type="text" data-bind="value: $data.email" />
    Ex: johndoe@ucsd.edu
    <br />
    <br />
    Password : <input type="text" data-bind="value: $data.password" />
    must be at least 6 characters long
    <br />
    <br />
    <input type="button" data-bind="value: 'Update', click: $data.update" style="margin-left:80px" />
    <br /><br />
    <div id="divMessage"></div>    
</div>

<script type="text/javascript">
    $(document).ready(function () {
        // this require[] will load the /Scripts/136Libraries/ViewModels/AdminViewModel.js file
        // and return the object inside it and assign to adminViewModel variable.
        require(['ViewModels/AdminViewModel'], function (adminViewModel) {
            var viewModel = new adminViewModel();
            viewModel.Load(@id);
        });
    });

</script>
